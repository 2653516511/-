# 运算符优先级(Operator Precedence)

例：

1. 三元运算符：
    
    ```jsx
    const isKet = type === EXAM_TYPE.KET
    const examType = isKet ? 'ket' : 'pet'
    
    // 和
    const examType = isKet && 'ket' || 'pet'
    ```
    
2.